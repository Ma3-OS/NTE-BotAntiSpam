import platform

# ==========================================
# แผงควบคุม (Configuration)
# ==========================================

# 1. ระบบจัดการสแปม (True = เปิด / False = ปิด)
AUTO_MOD_ENABLED = True      # สแกนรูปภาพอัตโนมัติ
MANUAL_MOD_ENABLED = True    # เปิดให้แอดมินใช้คำสั่ง !despam
IMAGE_CACHE_ENABLED = True   # ระบบจำภาพที่เคยแบนแล้ว (ประหยัด RAM)

# 2. ตั้งค่าบอท
COMMAND_PREFIX = "!"         # สัญลักษณ์นำหน้าคำสั่ง
FUZZY_THRESHOLD = 85         # เปอร์เซ็นต์ความแม่นยำในการจับคำที่สะกดคล้ายกัน

# 3. ตั้งค่าระบบ AI
# เช็คระบบปฏิบัติการอัตโนมัติ
if platform.system() == 'Windows':
    TESSERACT_CMD = r'C:\Program Files\Tesseract-OCR\tesseract.exe'  # สำหรับรันบนคอมตัวเอง (Local)
else:
    TESSERACT_CMD = '/usr/bin/tesseract'  # สำหรับรันบน Render (Linux)

# ==========================================
# 4. ข้อความหน้างาน (สำหรับคำสั่ง !despam)
# ==========================================
MSG_DESPAM_DISABLED = "⚠️ ระบบ !despam ปิดใช้งานอยู่ครับ (เปิดได้ในไฟล์ config.py)"
MSG_NEED_REPLY = "❌ ต้องกดตอบกลับ (Reply) รูปภาพที่ต้องการให้บินครับ"
MSG_MSG_NOT_FOUND = "❌ หาข้อความต้นทางไม่เจอครับ (อาจจะโดนลบไปแล้ว)"
MSG_NO_IMAGE = "❌ ไม่มีรูปภาพในข้อความที่ตอบกลับครับ"

MSG_SCANNING = "⏳ กำลังตรวจสอบรูปภาพ... รอสักครู่นะครับ"
MSG_SAFE = "✅ ตรวจสอบแล้วไม่ใช่สแปมครับ ปลอดภัย!"

# (ตรง {reason} บอทจะเอาสาเหตุมาใส่แทนอัตโนมัติ)
MSG_SPAM_FOUND = "🚨 **เจอสแปม!** กำลังทำการบินข้อความและลงดาบ...\n> สาเหตุ: {reason}"

# (ตรง {error} บอทจะเอาข้อความ Error จากระบบมาใส่แทน)
MSG_ERROR = "❌ เกิดข้อผิดพลาดในการตรวจสอบ: {error}"
